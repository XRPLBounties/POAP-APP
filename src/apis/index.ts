import {
  mint,
  claim,
  startVerification,
  verifyOwnership,
  getEvent,
  getEvents,
  getUser,
  updateUser
} from "apis/poap";

const API = {
  mint,
  claim,
  startVerification,
  verifyOwnership,
  getEvent,
  getEvents,
  getUser,
  updateUser,
};

export default API;
