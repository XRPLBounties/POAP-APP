import {
  mint,
  claim,
  startVerification,
  verifyOwnership,
  event,
  events,
} from "apis/poap";

const API = {
  mint,
  claim,
  startVerification,
  verifyOwnership,
  event,
  events,
};

export default API;
