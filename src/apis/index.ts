import {
  mint,
  claim,
  startVerification,
  verifyOwnership,
  attendees,
} from "apis/poap";

const API = {
  mint,
  claim,
  startVerification,
  verifyOwnership,
  attendees,
};

export default API;
